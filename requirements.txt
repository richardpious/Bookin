# List your python dependencies here

# Backend
fastapi
uvicorn
pydantic

# System dependencies (install via OS package manager, not pip)
# ripgrep  ->  sudo apt-get install ripgrep  (Ubuntu/Debian)
#          ->  brew install ripgrep          (macOS)
