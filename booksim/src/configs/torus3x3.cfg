// 3x3 Torus configuration
topology = torus;
k = 3;
n = 2;

// Routing
routing_function = dim_order;

// Flow control
num_vcs = 2;

// Traffic
traffic = uniform;
injection_rate = 0.05;

// Simulation parameters
sim_count = 1;
sim_type = latency;
warmup_periods = 1000;
max_samples = 10000;
// Need to add duration if needed
// Let's assume default behavior for now
